<!DOCTYPE html>
<html lang="en">

    <header>
      <% include partials/_header%>
    </header>


  <body leftmargin="100px", topmargin="100px" style="font-size:20px; padding:50px;">

    <table>
        <tr>
          <td><strong>Shortened URL &#8594;</strong> </td>
          <td><strong>Original URL</strong></td>
          <td><strong>Actions</strong></td>
        </tr>
      <% for (var URL in urls) { %>
        <tr>
          <td><%= URL %> &#8594; </td>
          <td><%= urls[URL].longurl %> </td>
          <td>
            <a href="/urls/<%= URL %>">Edit</a>
            <form method="POST" action="/urls/<%= URL %>/delete">
              <input type="submit" value="Delete"> </form>
          </td>
        </tr>
      <% } %>

    </table>

  </br >
    <a href="/urls/new"> Generate Shortened URLS </a>


  </body>

</html>